<UserControl x:Class="PageNavigationApp.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PageNavigationApp.Views"
             xmlns:vm="clr-namespace:PageNavigationApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="622" d:DesignWidth="600"
             Style="{StaticResource Page_Style}">

    <UserControl.DataContext>
        <vm:ProductsViewModel />
    </UserControl.DataContext>

    <Grid>

        <Border CornerRadius="0,20,20,0">
            <Border.Background>
                <ImageBrush ImageSource="/Assets/Images/product_page.png"
                            Stretch="None" />
            </Border.Background>
        </Border>

        <TextBlock Text="{Binding ProductAvailability}"
                   Foreground="#F25E68"
                   FontSize="12"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   FontFamily="/Assets/Fonts/#Rubik Medium"
                   Margin="261,300,0,0" />


    </Grid>
</UserControl>
